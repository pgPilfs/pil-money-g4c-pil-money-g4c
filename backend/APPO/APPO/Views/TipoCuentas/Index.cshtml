@model IEnumerable<APPO.Models.TiposCuentas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Agregar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Modificar", new { id=item.id_tipo_cuenta }) |
            @Html.ActionLink("Details", "DetallesTiposCuentas", new { id=item.id_tipo_cuenta }) |
            @Html.ActionLink("Delete", "Eliminar", new { id=item.id_tipo_cuenta })
        </td>
    </tr>
}

</table>
